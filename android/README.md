# Android module (placeholder)

Kotlin/Compose application sources will live here.

Planned modules:
- `app` — UI + agent chat
- `accessibility` — AccessibilityService actions
- `planner` — local LLM bridge
- `audit` — on-device action log
