<?xml version="1.0" encoding="utf-8"?>
<!--
  Restrictive cleartext policy for local-first Ollama.
  Public cleartext is denied. Loopback / emulator / mDNS .local names are allowed.

  Android Network Security Config cannot express RFC1918 CIDR ranges as domains;
  application-layer NetworkPolicy.validateOllamaHost() still rejects non-local
  cleartext URLs before they are persisted or used. Prefer hostname.local or
  HTTPS when talking to a LAN Ollama host by raw private IP is blocked by NSC.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- mDNS / Bonjour LAN hosts (NetworkPolicy also allows *.local) -->
        <domain includeSubdomains="true">local</domain>
    </domain-config>
</network-security-config>
